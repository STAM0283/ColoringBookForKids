import { asc, eq, sql } from "drizzle-orm";
import { auth } from "@/auth";import { db } from "@/db";import { activityCategories, categories } from "@/db/schema";import { slugify } from "@/lib/utils";
const colorPattern=/^#[0-9A-F]{6}$/i;
export async function GET(){if((await auth())?.user.role!=="ADMIN")return Response.json({message:"Non autorisé."},{status:401});const items=await db.select({category:categories,activityCount:sql<number>`count(${activityCategories.activityId})`}).from(categories).leftJoin(activityCategories,eq(activityCategories.categoryId,categories.id)).where(eq(categories.scope,"ACTIVITY")).groupBy(categories.id).orderBy(asc(categories.sortOrder),asc(categories.name));return Response.json({items:items.map(({category,activityCount})=>({...category,activityCount:Number(activityCount)}))})}
export async function POST(request:Request){if((await auth())?.user.role!=="ADMIN")return Response.json({message:"Non autorisé."},{status:401});const body=await request.json().catch(()=>null)as{name?:string;description?:string;color?:string;badge?:string}|null;const name=body?.name?.trim(),color=body?.color?.toUpperCase()||"#0F8A68",badge=body?.badge?.trim()||"🏷️";if(!name||name.length<2||name.length>60)return Response.json({message:"Le nom doit contenir entre 2 et 60 caractères."},{status:400});if(!colorPattern.test(color))return Response.json({message:"Couleur invalide."},{status:400});if(badge.length>12)return Response.json({message:"Le badge est trop long."},{status:400});const id=crypto.randomUUID();try{await db.insert(categories).values({id,scope:"ACTIVITY",name,slug:slugify(name),description:body?.description?.trim()||null,color,badge});return Response.json({message:"Catégorie créée avec succès.",id},{status:201})}catch{return Response.json({message:"Cette catégorie existe déjà."},{status:409})}}
