import Link from "next/link";
import { Users, X } from "lucide-react";
type CharacterOption={name:string;slug:string;color:string};
export function CharacterFilter({options,selected,path,query,locale="fr"}:{options:CharacterOption[];selected?:string;path:string;query:{q?:string;category?:string;sort?:string};locale?:"fr"|"en"}){
 if(!options.length)return null;
 const hrefFor=(character?:string)=>{const p=new URLSearchParams();if(query.q)p.set("q",query.q);if(query.category)p.set("category",query.category);if(query.sort)p.set("sort",query.sort);if(character)p.set("character",character);return `${path}${p.size?`?${p}`:""}`};
 return <section aria-label={locale==="en"?"Filter by character":"Filtrer par personnage"} className="mb-5 rounded-[1.65rem] border border-emerald-900/10 bg-white/75 p-4 shadow-sm backdrop-blur dark:border-white/10 dark:bg-white/[.045]"><div className="mb-3 flex items-center justify-between gap-3"><div className="flex items-center gap-2"><span className="grid size-9 place-items-center rounded-xl bg-emerald-100 text-emerald-700 dark:bg-emerald-400/15 dark:text-emerald-300"><Users size={17}/></span><div><p className="text-[10px] font-black uppercase tracking-[.16em] text-primary">{locale==="en"?"Meet the cast":"Les héros de la galerie"}</p><h2 className="text-sm font-black">{locale==="en"?"Choose a character":"Choisir un personnage"}</h2></div></div>{selected&&<Link href={hrefFor()} className="inline-flex items-center gap-1 rounded-full px-2.5 py-1.5 text-xs font-bold text-foreground/55 hover:bg-foreground/5 hover:text-foreground"><X size={13}/>{locale==="en"?"Clear":"Effacer"}</Link>}</div><div className="flex flex-wrap gap-2">{options.map(option=>{const active=selected===option.slug;return <Link key={option.slug} href={hrefFor(active?undefined:option.slug)} aria-current={active?"true":undefined} className={`inline-flex items-center gap-2 rounded-full border px-3.5 py-2 text-sm font-extrabold transition ${active?"border-transparent bg-slate-950 text-white shadow-md dark:bg-white dark:text-slate-950":"border-foreground/10 bg-background/75 text-foreground/75 hover:-translate-y-0.5 hover:border-primary/35 hover:text-foreground hover:shadow-sm dark:bg-white/5"}`}><span className="size-2.5 rounded-full ring-2 ring-white/70 dark:ring-black/20" style={{backgroundColor:option.color}}/>{option.name}</Link>})}</div></section>;
}
