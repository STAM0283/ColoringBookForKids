import { eq, or } from "drizzle-orm";import { z } from "zod";import { auth } from "@/auth";import { db } from "@/db";import { activities,bookGallery,books,media,mediaCategories,posts,vlogs } from "@/db/schema";import { storageService } from "@/lib/storage/local-storage";
export const runtime="nodejs";async function admin(){return (await auth())?.user.role==="ADMIN"}
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){if(!await admin())return Response.json({message:"Non autorisé."},{status:401});const parsed=z.object({alt:z.string().max(300),categoryId:z.string().nullable().optional()}).safeParse(await request.json().catch(()=>null));if(!parsed.success)return Response.json({message:"Description invalide."},{status:400});const {id}=await params;await db.update(media).set({alt:parsed.data.alt.trim()||null,updatedAt:new Date()}).where(eq(media.id,id));if(parsed.data.categoryId!==undefined){await db.delete(mediaCategories).where(eq(mediaCategories.mediaId,id));if(parsed.data.categoryId)await db.insert(mediaCategories).values({mediaId:id,categoryId:parsed.data.categoryId})}return Response.json({message:"Image modifiée."})}
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){if(!await admin())return Response.json({message:"Non autorisé."},{status:401});const {id}=await params,item=await db.query.media.findFirst({where:eq(media.id,id)});if(!item)return Response.json({message:"Média introuvable."},{status:404});const references=await Promise.all([db.query.books.findFirst({where:or(eq(books.coverMediaId,id),eq(books.videoMediaId,id),eq(books.ogImageMediaId,id))}),db.query.activities.findFirst({where:or(eq(activities.previewMediaId,id),eq(activities.pdfMediaId,id))}),db.query.posts.findFirst({where:eq(posts.coverMediaId,id)}),db.query.vlogs.findFirst({where:or(eq(vlogs.videoMediaId,id),eq(vlogs.thumbnailMediaId,id))}),db.query.bookGallery.findFirst({where:eq(bookGallery.mediaId,id)})]);if(references.some(Boolean))return Response.json({message:"Ce média est utilisé par un contenu. Modifiez d’abord ce contenu."},{status:409});await db.delete(media).where(eq(media.id,id));await storageService.deleteFile(item.path).catch(()=>undefined);return Response.json({message:"Média supprimé."})}
