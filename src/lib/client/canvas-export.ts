function safeName(title:string){return title.normalize("NFD").replace(/[\u0300-\u036f]/g,"").replace(/[^a-z0-9]+/gi,"-").replace(/^-|-$/g,"").toLowerCase()||"mon-coloriage"}
function save(blob:Blob,name:string){const url=URL.createObjectURL(blob),link=document.createElement("a");link.href=url;link.download=name;link.click();setTimeout(()=>URL.revokeObjectURL(url),1000)}
export async function exportCanvasPng(canvas:HTMLCanvasElement,title:string){const blob=await new Promise<Blob|null>(resolve=>canvas.toBlob(resolve,"image/png"));if(blob)save(blob,`${safeName(title)}.png`)}
export async function exportImagePdf(source:string,title:string){const image=new Image();image.decoding="async";image.src=source;await image.decode();const canvas=document.createElement("canvas");canvas.width=image.naturalWidth;canvas.height=image.naturalHeight;const context=canvas.getContext("2d");if(!context)throw new Error("Canvas unavailable");context.fillStyle="#FFFFFF";context.fillRect(0,0,canvas.width,canvas.height);context.drawImage(image,0,0);await exportCanvasPdf(canvas,title)}
export async function exportCanvasPdf(canvas:HTMLCanvasElement,title:string){const jpeg=await new Promise<Blob|null>(resolve=>canvas.toBlob(resolve,"image/jpeg",.94));if(!jpeg)return;const image=new Uint8Array(await jpeg.arrayBuffer()),landscape=canvas.width>canvas.height,pageWidth=landscape?841.89:595.28,pageHeight=landscape?595.28:841.89,margin=28,scale=Math.min((pageWidth-margin*2)/canvas.width,(pageHeight-margin*2)/canvas.height),drawWidth=canvas.width*scale,drawHeight=canvas.height*scale,x=(pageWidth-drawWidth)/2,y=(pageHeight-drawHeight)/2,content=`q\n${drawWidth.toFixed(2)} 0 0 ${drawHeight.toFixed(2)} ${x.toFixed(2)} ${y.toFixed(2)} cm\n/Im0 Do\nQ`,encoder=new TextEncoder(),parts:Array<Uint8Array>=[],offsets:number[]=[0];let length=0;const add=(value:string|Uint8Array)=>{const bytes=typeof value==="string"?encoder.encode(value):value;parts.push(bytes);length+=bytes.length};add("%PDF-1.4\n%\xE2\xE3\xCF\xD3\n");const object=(id:number,body:string|(()=>void))=>{offsets[id]=length;add(`${id} 0 obj\n`);if(typeof body==="string"){add(body)}else{body()}add("\nendobj\n")};object(1,"<< /Type /Catalog /Pages 2 0 R >>");object(2,"<< /Type /Pages /Kids [3 0 R] /Count 1 >>");object(3,`<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageWidth.toFixed(2)} ${pageHeight.toFixed(2)}] /Resources << /XObject << /Im0 4 0 R >> >> /Contents 5 0 R >>`);object(4,()=>{add(`<< /Type /XObject /Subtype /Image /Width ${canvas.width} /Height ${canvas.height} /ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${image.length} >>\nstream\n`);add(image);add("\nendstream")});object(5,`<< /Length ${encoder.encode(content).length} >>\nstream\n${content}\nendstream`);const xref=length;add(`xref\n0 6\n0000000000 65535 f \n`);for(let id=1;id<=5;id++)add(`${String(offsets[id]).padStart(10,"0")} 00000 n \n`);add(`trailer\n<< /Size 6 /Root 1 0 R >>\nstartxref\n${xref}\n%%EOF`);const pdf=new Uint8Array(length);let cursor=0;for(const part of parts){pdf.set(part,cursor);cursor+=part.length}save(new Blob([pdf],{type:"application/pdf"}),`${safeName(title)}.pdf`)}
