import {Download,FileImage,FileText} from "lucide-react";
import {exportCanvasPdf,exportCanvasPng} from "@/lib/client/canvas-export";
export function ExportColoringButtons({title,en,getCanvas}:{title:string;en:boolean;getCanvas:()=>Promise<HTMLCanvasElement|null>|HTMLCanvasElement|null}){async function run(format:"png"|"pdf"){const canvas=await getCanvas();if(!canvas)return;if(format==="png"){await exportCanvasPng(canvas,title);return}await exportCanvasPdf(canvas,title)}return <section className="rounded-[1.4rem] border bg-card p-3 dark:border-white/10"><p className="flex items-center gap-2 px-1 text-xs font-black uppercase tracking-wide text-foreground/55"><Download size={15}/>{en?"Save my picture":"Enregistrer mon dessin"}</p><div className="mt-3 grid grid-cols-2 gap-2"><button type="button" onClick={()=>void run("png")} className="inline-flex min-h-12 items-center justify-center gap-2 rounded-xl bg-fuchsia-700 px-3 text-xs font-black text-white shadow-md transition hover:bg-fuchsia-600"><FileImage size={17}/>{en?"Image":"Image PNG"}</button><button type="button" onClick={()=>void run("pdf")} className="inline-flex min-h-12 items-center justify-center gap-2 rounded-xl bg-slate-950 px-3 text-xs font-black text-white shadow-md transition hover:bg-slate-800 dark:bg-white dark:text-slate-950"><FileText size={17}/>PDF</button></div></section>}
